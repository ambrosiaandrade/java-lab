package design_patterns.singleton.todo;

/**
 * Implementar o padrão Singleton em Java com `volatile`, `synchronized` e
 * `Double-Checked Locking`
 */
public class Singleton2 {

    // Atributo estático com `volatile` que contém a instância única

    // Construtor privado para evitar instanciamento externo

    // Método estático com `synchronized` e `Double-Checked Locking` para obter a instância única

}
